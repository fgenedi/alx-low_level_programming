#include <stdio.h>
#include "main.h"
/**
 * _abs - returns the absolute value
 * @n: the int to return its absolute
 * Return: absolute value
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
