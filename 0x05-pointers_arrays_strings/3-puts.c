#include "main.h"
#include <stdio.h>
/**
 * _puts - prints string
 *
 * @str: string
 * Return: void.
 */
void _puts(char *str)
{
while (1)
{
if (*s == 0)
{
_putchar (10);
return;
}
_putchar (*s);
s++;
}
}
