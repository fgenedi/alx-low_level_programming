#include "main.h"
#include <stdio.h>
/**
 * _isdigit - check the entry to determine if is lower or uppercase
 *
 * @c: checked
 * Return: boolean.
 */
int _isdigit(int c)
{
return (c >= 48 && c <= 57);
}
