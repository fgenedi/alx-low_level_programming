#include "main.h"
#include <stdio.h>
/**
 * _isupper - check the entry to determine if is lower or uppercase
 *
 * @c: checked
 * Return: boolean.
 */
int _isupper(int c)
{
return (c >= 65 && c <= 90);
}
