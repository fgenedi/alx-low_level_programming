#include "main.h"
#include <stdio.h>
/**
 * print_square - entry point
 *
 * @size: how many #
 * Return: void
 */
void print_square(int size)
{
int x;
int i;
for(x = 0; x < n; x++)
{
for(i = 0; i < n; i++)
{
_putchar(35);
}
_putchar(10);
}
}
